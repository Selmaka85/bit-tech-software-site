# .ai_ram.md — web/ local pointer

## PROJECT

Deployable Next.js marketing app for Bit-Tech Software personal site.

## CURRENT STATE

MVP complete. Single page `/`. Build passes. Awaiting deploy.

## LOCAL BOILERPLATE

Parent project memory: `../.scaffold_driven_boilerplate.md`

Content: `lib/site-data.ts`

## VERIFICATION

```powershell
npm run dev    # http://localhost:3000
npm run build
```

## NEXT

See parent `../.task_board.md` — SITE-006 deploy.

## FUTURE-TECH MODE (2026-06-24)

Optional visual layer toggled from header button **Future-Tech Mode**.

- Default: green premium SaaS theme (unchanged from v1 backup).
- Enabled: cyan/violet operator-lab aesthetic via CSS variables on `html[data-future-tech="true"]`.
- Preference persisted in `localStorage` key `bit-tech-future-tech-mode`.
- Same content (pricing, stack, copy) — facade only.

Rollback to pre-toggle site: `../backups/web-v1-clean-premium-2026-06-24/`
