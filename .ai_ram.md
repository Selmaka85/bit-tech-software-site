# .ai_ram.md — web/ local pointer

## PROJECT

Bit-Tech Software marketing site. Live: `https://bit-tech-software.co.uk`.

## CURRENT STATE

Nocturne: skin via Design Modes; **FX toggle** (blood/bats/spider) via castle panel — does NOT switch to Core. FX portaled to `body` z-80; CSS blood fallback; reduced-motion no longer hides FX.

## STORAGE

- `bit-tech-site-theme`
- `bit-tech-nocturne-fx` (`true`/`false`)

## VERIFY

`npm run build` PASS
