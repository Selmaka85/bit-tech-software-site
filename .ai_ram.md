# .ai_ram.md — web/ local pointer

## PROJECT

Deployable Next.js marketing app for Bit-Tech Software. Production: `https://bit-tech-software.co.uk`.

## CURRENT STATE

**Local (pending push):** 6-theme engine — Core / Future / Executive / Clinical / Industrial / Noir via ThemeRosette. Same copy; CSS tokens only. `localStorage` key `bit-tech-site-theme`.

**Live:** `da32d1b` agency pricing.

## THEMES

- `lib/themes.ts` — ids + icons
- `components/ThemeEngine.tsx` — shell, rosette, ambient (Future only)
- `styles/future-tech.css` — `data-theme="future"`
- `styles/site-themes.css` — Executive / Clinical / Industrial / Noir + rosette UI
- Header uses `ThemeRosette` (replaces FT Mode toggle)

## VERIFICATION

`npm run build` — PASS

## NEXT

User preview → push when approved.
